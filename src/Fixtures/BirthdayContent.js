export const dateArray = Array(31)
  .fill(1)
  .map((element, index) => element + index);

export const monthArray = [
  "January",
  "February",
  "March",
  "April",
  "May",
  "Jun",
  "July",
  "August",
  "September",
  "October",
  "November",
  "December",
];

export const yearArray = Array(45)
  .fill(1980)
  .map((year, index) => year + index);
